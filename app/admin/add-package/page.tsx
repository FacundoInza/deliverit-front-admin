import React, { FC } from 'react';
import { AddPackages } from '../../../components/ui/add-packages/AddPackages';

const AddPackagesRoute: FC = () => {
    return (
        <div>
            <AddPackages />
        </div>
    );
};

export default AddPackagesRoute;
